<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console-only: Render captures stdout natively. No FILE appender here —
         the hosted filesystem is ephemeral (wiped on restart/redeploy) and
         there's no Promtail sidecar possible on a single free web service. -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{correlationId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
