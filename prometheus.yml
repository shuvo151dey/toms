global:
  scrape_interval: 15s

scrape_configs:
  - job_name: toms-backend
    metrics_path: /actuator/prometheus
    static_configs:
      # The backend runs on the host, not in Docker — host.docker.internal
      # is how a container reaches the host machine
      - targets: ["host.docker.internal:8080"]
